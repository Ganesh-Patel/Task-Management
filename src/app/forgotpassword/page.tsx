import React from 'react';
import ForgetPassword from '@/components/auth/ForgetPassword'; // Adjust the path if needed

const ForgetPasswordPage = () => {
    return (
        <main>
            <ForgetPassword />
        </main>


    );
};

export default ForgetPasswordPage;
