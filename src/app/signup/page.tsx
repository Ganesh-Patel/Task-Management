import React from 'react';
import Signup from '../../components/auth/Signup'; // Adjust the path if needed

const SignupPage = () => {
    return (
      <main>
         <Signup />
      </main>
    );
};

export default SignupPage;
