/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.shutterstock.com', 'i.pinimg.com','res.cloudinary.com'],
    },
  };
  
  export default nextConfig;
  